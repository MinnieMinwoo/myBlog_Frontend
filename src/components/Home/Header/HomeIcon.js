import styles from "./HomeIcon.module.css";

const HomeIcon = () => {
  return (
    <div className={`HomeIcon ${styles.HomeIcon}`}>
      <button></button>
      <button></button>
    </div>
  );
};

export default HomeIcon;
